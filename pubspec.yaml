name: coupon_mobile_app
description: A premium mobile coupon sales and inventory management client for CouponOS.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.6.0
  provider: ^6.1.2
  http: ^1.2.0
  shared_preferences: ^2.2.3
  google_fonts: ^8.1.0
  intl: ^0.19.0
  fl_chart: ^0.65.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
